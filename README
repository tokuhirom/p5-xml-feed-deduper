This is Perl module XML::Feed::Deduper.

INSTALLATION

XML::Feed::Deduper installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan XML::Feed::Deduper

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

XML::Feed::Deduper documentation is available as in POD. So you can do:

    % perldoc XML::Feed::Deduper

to read the documentation online with your favorite pager.

Tokuhiro Matsuno
